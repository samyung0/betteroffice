{
  "name": "@betteroffice/fidelity",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "bun build src/app.ts --outdir dist --target browser --minify && cp src/index.html dist/index.html",
    "predev": "bun run build",
    "dev": "wrangler dev",
    "predeploy": "bun run build",
    "deploy": "wrangler deploy",
    "test": "bun test src",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.worker.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.106.0"
  }
}
