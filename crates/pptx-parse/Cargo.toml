[package]
name = "betteroffice-pptx-parse"
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
publish = ["crates-io"]
description = "Bounded PresentationML parsing and part-preserving PPTX package serialization."
readme = "README.md"
keywords = ["pptx", "powerpoint", "ooxml", "presentationml"]
categories = ["parser-implementations", "encoding"]

[lib]
name = "pptx_parse"

[dependencies]
base64 = "0.22"
ooxml-drawingml = { workspace = true, features = ["chart"] }
ooxml-opc.workspace = true
quick-xml = { version = "0.41", features = ["encoding"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
proptest = "1"
serde_json = "1"
