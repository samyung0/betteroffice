[package]
name = "betteroffice-ooxml-fidelity"
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
publish = false
description = "Fidelity oracles over package bytes: structural fingerprint, WML semantic digest, element census."

[lib]
name = "ooxml_fidelity"

[dependencies]
quick-xml = { version = "0.41", features = ["encoding"] }
serde_json = "1"
thiserror = "2"
