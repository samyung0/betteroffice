[package]
name = "betteroffice-docx-layout"
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
publish = ["crates-io"]
description = "The docx layout core in Rust, compiled to WASM: pagination, line flow, tables, floats, notes, and display lists behind the MeasuredBlock[] -> Layout contract, gated by the golden corpus."
readme = "README.md"
keywords = ["docx", "word", "ooxml", "layout"]
categories = ["rendering", "text-processing"]

[lib]
name = "docx_layout"
crate-type = ["cdylib", "rlib"]

# wasm-pack runs a wasm-opt post-pass after rustc/LLVM codegen. Pin it to -O3
# (speed) here so the layout wasm — the per-keystroke display-list hot path — is
# optimized for latency rather than the size-leaning default. This is read only
# for this crate's `wasm-pack build`, so the container build is unaffected. See
# scripts/embed-layout-wasm.mjs, which drives the build.
[package.metadata.wasm-pack.profile.release]
wasm-opt = ['-O3']

[dependencies]
ooxml-drawingml = { workspace = true, features = ["chart"] }
ooxml-text.workspace = true
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
unicode-segmentation = "1.12"
zmij = "1"

[dev-dependencies]
# the glyph-run emission tests build an explicit FontStore (register the
# Liberation fixture) and drive build_display_list_json_with_fonts directly
ooxml-text.workspace = true
